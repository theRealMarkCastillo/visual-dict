{
  "entities": {
    "UserProfile": {
      "title": "UserProfile",
      "description": "User account and profile record created upon Google account sign in",
      "type": "object",
      "properties": {
        "uid": {
          "type": "string",
          "description": "Unique identifier of the user matching Firebase Auth UID",
          "maxLength": 128
        },
        "email": {
          "type": "string",
          "description": "User email address from Google Account",
          "maxLength": 320
        },
        "displayName": {
          "type": "string",
          "description": "User display name from Google Account",
          "maxLength": 128
        },
        "photoURL": {
          "type": "string",
          "description": "User avatar image URL from Google Account",
          "maxLength": 1024
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp when account was first created"
        },
        "lastLoginAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp of most recent login"
        }
      },
      "required": [
        "uid",
        "email"
      ]
    }
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "UserProfile",
      "description": "Individual user document containing profile information"
    }
  }
}
